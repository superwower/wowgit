import GitService from "./git_service";

describe("isGitRepository", () => {
  it("returns false when a given path is not a git repository", () => {
    // TODO
  });
  it("returns true when a given path is a git repository", () => {
    // TODO
  });
});
